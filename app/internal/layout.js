export const metadata = {
  title: "Internal Conversion Portal | Smart Movers",
  robots: {
    index: false,
    follow: false,
    nocache: true,
  },
};

export default function InternalLayout({ children }) {
  return children;
}
